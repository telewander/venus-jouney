# Set SITE_URL as an environment variable in Amplify.
# Local builds read the shell environment (not this file).
# Example only — use your actual Amplify URL or your owned custom domain.
SITE_URL=https://your-production-domain.com
